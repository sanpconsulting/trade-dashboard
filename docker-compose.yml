version: '3.8'

services:
  trade-dashboard:
    build: .
    container_name: trade-dashboard
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
